# 项目目录说明

本项目采用“一道题一个文件夹”的结构，题目目录统一使用 `题目A1`、`题目A2`、`题目B1` 这类名称，便于按题号查找和提交。

## 目录职责

| 目录 | 内容 | 入口 |
|---|---|---|
| `题目A1` | Three.js 三维场景、Node 服务、Python 外部控制器和运行证据 | `server.js`、`controller.py` |
| `题目A2` | A1 在 WSL 中的部署、端口和浏览器访问说明 | `使用说明.md` |
| `题目A3` | Go TCP Server、Client、协议测试和运行证据 | `cmd/server`、`cmd/client` |
| `题目B1` | B1 独立运行入口和运行证据 | `solve.py` |
| `题目B2` | B2 独立运行入口和运行证据 | `solve.py` |
| `题目B3` | B3 独立运行入口和运行证据 | `solve.py` |
| `公共` | B1-B3 共用的刚体变换算法、测试和总报告 | `robot_b_questions.py` |

## 目录约定

```text
F:\实践考题\
  题目A1\
  题目A2\
  题目A3\
  题目B1\
  题目B2\
  题目B3\
  公共\
```

- 题目源码、题目说明和对应运行证据放在题目自己的目录内。
- `题目A2` 是 A1 的 WSL 部署题，部署的实际源码位于 `题目A1`，A2 不重复复制源码。
- `公共` 只保存 B1-B3 共享的数学实现和测试，不混放具体题目的证据。
- `node_modules`、Go 的 `bin` 和运行日志属于本地生成内容，分别由题目目录中的 `.gitignore` 排除。
- 当前 Python 实现只使用标准库，不需要 `requirements.txt`。
- 根目录的 `机器人实践考题.pdf` 是原题，`机器人实践考题_解题方案.md` 是总方案。
