# 机器人应用软件工程师实践考题解题方案

## 1. 文档目的

本文档根据《机器人实践考题》整理实现路线、数学约定、参考计算结果、测试方法和最终提交清单。

题目允许从 0 到 6 个部分中自主选择。建议优先完成 B1-B3，再完成 A1-A2，最后根据时间决定是否完成 A3。B1-B3 可以复用同一套刚体变换基础库，能够形成完整、连续的机器人应用能力证明。

本文档中的数值结果用于开发过程中的基准校验，最终应以程序实际输出和自动化测试结果为准。

## 2. 推荐完成范围

| 优先级 | 题目 | 推荐结论 | 原因 |
|---|---|---|---|
| 1 | B1 反解法兰位姿 | 必做 | 题目边界清楚，是后续 B2、B3 的基础 |
| 2 | B2 正解与下料反解 | 必做 | 能体现坐标链组合、缓存工具链和回代验证 |
| 3 | B3 Eye-to-hand 视觉定位 | 必做 | 能体现相机内参、手眼外参和机器人目标位姿计算 |
| 4 | A1 Three.js 外部驱动 | 推荐 | 有可视化结果，便于展示工程实现能力 |
| 5 | A2 Linux 部署 | 推荐 | 与 A1 配套，形成完整运行证据 |
| 6 | A3 Go TCP 聊天程序 | 时间充足时完成 | 能展示新语言学习和网络协议设计能力，但与机器人主题关联较弱 |

## 3. 统一坐标与数学约定

### 3.1 单位和姿态

- 平移单位：米 `m`。
- 题面角度单位：度 `deg`。
- 程序内部角度统一转换为弧度 `rad`。
- RPY 采用固定角 ZYX：

```text
R = Rz(rz) * Ry(ry) * Rx(rx)
```

- 四元数顺序固定为：

```text
q = (x, y, z, w)
```

### 3.2 齐次变换含义

记 `T_A_B` 表示坐标系 B 在坐标系 A 下的位姿。它可以把 B 系中的点转换到 A 系：

```text
p_A = T_A_B * p_B
```

变换链满足：

```text
T_A_C = T_A_B * T_B_C
```

若：

```text
T_A_B = [ R  t ]
        [ 0  1 ]
```

则逆变换为：

```text
inverse(T_A_B) = [ R^T  -R^T*t ]
                 [  0       1   ]
```

### 3.3 统一变量命名

建议使用能够直接表达方向的变量名，避免仅使用上下标导致理解错误：

```python
base_to_flange
flange_to_gripper
gripper_to_object
base_to_object
```

统一的刚性夹持关系为：

```text
T_base_object =
    T_base_flange
    * T_flange_gripper
    * T_gripper_object
```

因此法兰反解公式为：

```text
T_base_flange =
    T_base_object
    * inverse(T_flange_gripper)
    * inverse(T_gripper_object)
```

当工件坐标系与夹持中心重合时：

```text
T_gripper_object = I
```

## 4. B 题实现方案

### 4.1 建议目录

```text
robot-practice/
  README.md
  robot_math/
    transforms.py
    solve_exam.py
    test_transforms.py
    requirements.txt
```

核心模块建议提供以下函数：

```python
rot_x(angle_rad)
rot_y(angle_rad)
rot_z(angle_rad)
rpy_zyx_to_matrix(rx, ry, rz)
matrix_to_rpy_zyx(rotation)
matrix_to_quaternion(rotation)
quaternion_to_matrix(quaternion)
pose_to_matrix(position, rpy)
matrix_to_pose(transform)
compose(first, second)
inverse(transform)
```

### 4.2 B1：已知工件位姿反解法兰

已知：

```text
T_flange_gripper:
平移 (0.000, 0.000, 0.165) m
RPY  (0, 0, 45) deg

T_base_object:
平移 (0.520, -0.180, 0.120) m
RPY  (180, 0, -30) deg
```

题目假定工件坐标系与夹爪夹持中心重合，因此：

```text
T_gripper_object = I
```

计算公式：

```text
T_base_flange =
    T_base_object
    * inverse(T_flange_gripper)
```

参考结果：

```text
法兰平移：
(0.520000, -0.180000, 0.285000) m

法兰 RPY：
(180, 0, 15) deg
```

旋转矩阵约为：

```text
[ 0.965925826   0.258819045   0.000000000 ]
[ 0.258819045  -0.965925826   0.000000000 ]
[ 0.000000000   0.000000000  -1.000000000 ]
```

四元数 `(x, y, z, w)` 约为：

```text
(0.991445, 0.130526, 0.000000, 0.000000)
```

需要使用两条独立路径计算：

1. 直接使用旋转矩阵构造、求逆和相乘。
2. 将 RPY 转成四元数，使用四元数乘法和四元数求逆计算旋转，再与平移部分组合。

两条路径的旋转矩阵和位姿结果应满足：

```text
max(abs(result_matrix - cross_check_matrix)) < 1e-9
```

### 4.3 B2：正解、工具链缓存和下料反解

已知：

```text
T_base_flange:
平移 (0.420, 0.150, 0.560) m
RPY  (150, 0, 45) deg

T_flange_gripper:
平移 (0.000, 0.000, 0.165) m
RPY  (0, 0, 90) deg

T_gripper_object:
平移 (0.025, 0.000, 0.060) m
RPY  (0, 0, 0) deg
```

#### 当前夹持中心位姿

```text
T_base_gripper =
    T_base_flange
    * T_flange_gripper
```

参考结果：

```text
平移：
(0.478336, 0.091664, 0.417106) m

RPY：
(180, -30, -45) deg
```

#### 当前工件位姿

```text
T_base_object =
    T_base_gripper
    * T_gripper_object
```

参考结果：

```text
平移：
(0.514859, 0.055141, 0.377644) m

RPY：
(180, -30, -45) deg
```

#### 合并固定工具链

工程上可将固定链缓存为：

```text
T_flange_object =
    T_flange_gripper
    * T_gripper_object
```

参考结果：

```text
平移：
(0.000000, 0.025000, 0.225000) m

RPY：
(0, 0, 90) deg
```

缓存该结果的好处：

- 运行时只需要一次 `T_base_flange * T_flange_object`。
- 避免每次运动重复计算固定安装关系。
- 使工具坐标系或 TCP 标定结果集中管理。
- 便于替换工具或重新标定。
- 降低运行时公式书写错误的概率。

#### 下料目标法兰

目标工件位姿：

```text
T_base_object_target:
平移 (0.350, -0.450, 0.090) m
RPY  (180, 0, 0) deg
```

使用缓存工具链反解：

```text
T_base_flange_target =
    T_base_object_target
    * inverse(T_flange_object)
```

参考结果：

```text
平移：
(0.325000, -0.450000, 0.315000) m

RPY：
(180, 0, 90) deg
```

四元数 `(x, y, z, w)` 约为：

```text
(0.707107, 0.707107, 0.000000, 0.000000)
```

回代验证：

```text
T_base_flange_target
    * T_flange_object
≈ T_base_object_target
```

最大绝对误差应小于 `1e-9`。

### 4.4 B3：Eye-to-hand 视觉引导定位

已知相机内参：

```text
图像尺寸：1280 x 720
fx = 800.0
fy = 800.0
cx = 640.0
cy = 360.0
```

已知相机外参：

```text
T_base_camera:
平移 (0.700, -0.200, 1.000) m
RPY  (180, 0, 15) deg
```

视觉结果：

```text
像素 (u, v) = (720, 280)
Z_camera = 0.400 m
```

夹爪安装关系与 B1 相同：

```text
T_flange_gripper:
平移 (0.000, 0.000, 0.165) m
RPY  (0, 0, 45) deg
```

#### 像素反投影

使用针孔模型：

```text
x_camera = (u - cx) * Z_camera / fx
y_camera = (v - cy) * Z_camera / fy
z_camera = Z_camera
```

参考结果：

```text
p_camera = (0.040000, -0.040000, 0.400000) m
```

#### 相机系转换到基座系

```text
p_base =
    R_base_camera * p_camera
    + t_base_camera
```

参考结果：

```text
p_base = (0.728284, -0.151010, 0.600000) m
```

此处不需要机器人当前位姿，因为相机采用 eye-to-hand 固定安装方式，相机相对基座的外参是固定的。机器人当前位姿只在后续将目标工件位姿反解为法兰目标位姿时发挥作用，且反解本身不需要当前姿态作为输入。

#### 构造工件完整位姿

圆盘平放在水平工作台上，法向竖直，题目允许绕基座 Z 轴任意旋转。因此可选：

```text
T_base_object:
平移 (0.728284, -0.151010, 0.600000) m
RPY  (180, 0, 0) deg
```

圆盘绕自身法向旋转对称，绕基座 Z 轴的角度不会改变抓取结果，所以视觉不需要提供类似长条工件的长轴方向。

#### 反解抓取法兰

```text
T_base_flange_target =
    T_base_object
    * inverse(T_flange_gripper)
```

参考结果：

```text
平移：
(0.728284, -0.151010, 0.765000) m

RPY：
(180, 0, 45) deg
```

四元数 `(x, y, z, w)` 约为：

```text
(0.923880, 0.382683, 0.000000, 0.000000)
```

回代验证：

```text
T_base_flange_target
    * T_flange_gripper
≈ T_base_object
```

同时应验证夹持中心平移位置与圆盘中心的误差小于 `1e-9`。

## 5. B 题测试方案

### 5.1 基础单元测试

- `R * R.T` 接近单位矩阵。
- 旋转矩阵行列式接近 `1`。
- `T * inverse(T)` 接近 4x4 单位矩阵。
- RPY 转矩阵后再转 RPY，旋转矩阵保持一致。
- 四元数转矩阵后再转四元数，旋转矩阵保持一致。
- 平移量不因姿态转换而发生单位变化。

### 5.2 题目级测试

每道题至少验证：

1. 结果平移、旋转矩阵、RPY、四元数均输出。
2. 矩阵路径和四元数路径交叉验证。
3. 反解结果重新正解后与目标结果比较。
4. 最大绝对误差小于 `1e-9`。

### 5.3 等价姿态处理

应注意以下数学等价性：

- 四元数 `q` 和 `-q` 表示同一个旋转。
- RPY 可能存在多组等价表示。
- 因此最终比较应优先使用旋转矩阵，或者比较四元数对应的旋转结果，不应只比较 RPY 字符串。

## 6. A1：Three.js 3D 场景与外部驱动

### 6.1 技术选型

- 前端：Vite + Three.js。
- 外部控制：优先使用 WebSocket。
- 控制程序：Python 或 Node.js 均可。
- 页面单位：建议使用米，场景中可设置合适的显示缩放。

### 6.2 页面功能

场景至少包含：

- 透视相机。
- 光源。
- 三维坐标轴。
- 网格地面。
- 可沿路径移动的方块。
- 目标位置标记。
- 实际运动轨迹线。
- 当前坐标、目标坐标和速度显示。
- WebSocket 连接状态。

### 6.3 控制消息

建议定义如下 JSON 消息：

```json
{
  "type": "move",
  "target": {
    "x": 1.0,
    "y": 0.5,
    "z": 0.2
  },
  "speed": 0.4,
  "commandId": "cmd-001"
}
```

字段含义：

| 字段 | 含义 |
|---|---|
| `type` | 消息类型 |
| `target` | 目标位置 |
| `speed` | 移动速度，单位为 m/s |
| `commandId` | 用于跟踪命令执行状态 |

页面按每帧时间差执行：

```text
step = speed * deltaTime
```

当当前位置到目标位置的距离小于步长时，直接设置为目标位置并发送完成事件；否则沿当前位置到目标位置的方向移动。

### 6.4 路径处理

题面没有给出具体路径坐标，需要在 README 中声明一条自定义路径，例如：

```text
P0 -> P1 -> P2 -> P3
```

每个路径点应明确坐标和单位。外部程序可以按顺序发送路径点，页面逐点执行，形成完整的路径运动效果。

## 7. A2：Linux 部署

### 7.1 推荐环境

优先使用 WSL2 Ubuntu，原因是：

- 不需要额外购买云服务器。
- 可以在 Windows 宿主机上快速复现。
- 便于展示 Linux 进程、端口和浏览器访问结果。

如果使用虚拟机，应在文档中明确选择：

- 桥接网卡；或
- NAT + 端口转发。

### 7.2 部署步骤

```bash
cd /mnt/f/实践考题/threejs_control

npm install
node server.js --host 0.0.0.0 --port 8080

# 启动外部控制服务
python controller.py
```

本实现使用 Node 标准库直接提供网页、HTTP 控制接口和 WebSocket，不需要执行 `npm run build` 或 `npm run preview`。Three.js 通过 Node 服务从本地 `node_modules/three` 提供，不依赖浏览器访问外部 CDN。

浏览器访问：

```text
http://localhost:8080/
```

服务监听 `0.0.0.0`，便于 Windows 宿主机通过 WSL localhost forwarding 访问。修改服务端或前端文件后，需要停止旧进程并重新启动 Node 服务，再使用 `Ctrl+F5` 刷新浏览器。

### 7.3 运行证据

至少准备以下截图：

1. Linux 终端显示服务进程和监听端口。
2. 宿主机浏览器成功访问网页。
3. 外部程序发送移动命令后的场景状态。
4. 方块到达目标点后的结果。

## 8. A3：Go TCP 多客户端聊天程序

### 8.1 Server 功能

- 监听 TCP 端口。
- 支持多个 Client 同时在线。
- 用户名注册和重复用户名检查。
- 维护在线用户表。
- 点对点消息转发。
- 客户端断开后清理连接。
- 向发送方返回目标用户不存在或发送失败的错误。

### 8.2 Client 功能

- 命令行参数指定用户名、服务器地址和端口。
- 能发送私聊消息。
- 能异步接收服务端转发的消息。
- 支持启动多个实例。
- 连接断开时给出明确错误。

### 8.3 推荐协议

采用“固定长度头 + JSON 消息体”：

```text
4 字节：消息体长度，大端序
N 字节：JSON 消息体，UTF-8 编码
```

消息体至少包含：

```json
{
  "type": "chat",
  "sender": "client-a",
  "receiver": "client-b",
  "contentLength": 12,
  "content": "hello client"
}
```

`contentLength` 应按 UTF-8 编码后的字节长度计算，而不是按 Unicode 字符数计算。

### 8.4 TCP 粘包和拆包

TCP 是字节流，没有消息边界。接收方必须：

1. 使用 `io.ReadFull` 读取固定的 4 字节长度。
2. 将长度解析为整数。
3. 按长度使用 `io.ReadFull` 读取完整 JSON 消息体。
4. 校验长度上限，防止异常数据导致过量内存分配。
5. 解析 JSON 并校验必填字段。

发送方使用一次完整的“长度头 + 消息体”写入逻辑，接收方仍然必须按协议拆包，不能假设一次 `Read` 就能读到完整消息。

## 9. README 必须包含的内容

- 项目背景和完成范围。
- 环境要求和安装命令。
- 启动命令。
- 坐标系定义和姿态约定。
- B1-B3 的推导公式。
- 关键中间结果和最终结果。
- 矩阵路径与四元数路径的交叉验证结果。
- 回代误差结果。
- A1 外部控制协议。
- A1 自定义路径点说明。
- A2 Linux 网络模式及选择原因。
- A3 TCP 粘包/拆包处理说明。
- 截图或录屏索引。
- 已知限制和未进行的硬件级验证。

## 10. 推荐提交结构

```text
robot-practice/
  README.md

  robot_math/
    transforms.py
    solve_exam.py
    test_transforms.py
    requirements.txt

  threejs-control/
    frontend/
    controller/
    protocol.md

  chat-go/
    cmd/
      server/
      client/
    internal/
      protocol/
      server/
      client/

  deploy/
    README.md
    start.sh
    stop.sh

  evidence/
    b1-result.png
    b2-result.png
    b3-result.png
    threejs-control.png
    linux-deployment.png
    chat-server-client.png
```

## 11. 完成顺序与验收清单

### 第一阶段：B 题基础库

- [ ] 实现旋转矩阵和齐次变换矩阵。
- [ ] 实现 RPY-ZYX 转换。
- [ ] 实现四元数 `(x, y, z, w)` 转换。
- [ ] 实现矩阵乘法和逆变换。
- [ ] 完成基础单元测试。

### 第二阶段：B1-B3 题目计算

- [ ] 完成 B1 法兰位姿反解。
- [ ] 完成 B2 当前工件正解。
- [ ] 完成工具链缓存。
- [ ] 完成 B2 下料法兰反解。
- [ ] 完成 B3 像素反投影。
- [ ] 完成相机系到基座系转换。
- [ ] 完成 B3 抓取法兰反解。
- [ ] 输出所有误差和回代结果。

### 第三阶段：A1-A2 工程展示

- [ ] 完成 Three.js 场景。
- [ ] 完成 WebSocket 控制。
- [ ] 完成自定义路径运动。
- [ ] 在 Linux 环境启动服务。
- [ ] 完成浏览器访问验证。
- [ ] 完成进程和端口截图。

### 第四阶段：A3 网络程序

- [x] 完成 Go Server。
- [x] 完成 Go Client。
- [x] 完成多客户端通信。
- [x] 完成协议编码和解码。
- [x] 完成粘包/拆包测试。
- [x] 完成断线和异常输入处理。

### 第五阶段：提交前检查

- [ ] 从空环境重新执行安装和启动命令。
- [ ] README 命令与实际目录一致。
- [ ] 所有截图能够证明对应功能确实运行。
- [ ] 清理临时文件和无关依赖。
- [ ] 检查 Git 仓库中没有敏感信息。
- [ ] 提交代码、文档、截图或录屏。
- [ ] 准备 GitHub/Gitee 仓库地址和线上访问地址。

## 12. 题面风险与注意事项

### 12.1 A1 路径未指定

题面只要求“沿给定路径移动”，但没有给出具体路径点。实现时应自定义路径，并在 README 中明确说明这是为完成演示而设定的测试路径。

### 12.2 RPY 和四元数存在多解

同一个旋转可能对应不同的 RPY 表示；四元数 `q` 与 `-q` 也表示同一个旋转。验收时应以旋转矩阵和回代误差为主要判断依据。

### 12.3 静态计算不等于机器人可执行

本题只验证数学变换和软件逻辑，不代表目标法兰位姿一定满足具体机器人的：

- 关节限位。
- 奇异位形限制。
- 碰撞约束。
- 工具姿态限制。
- 速度、加速度和安全区域限制。

如果没有真实机器人或仿真环境，README 中应明确说明尚未进行机器人运动学、碰撞和硬件级验证。

### 12.4 深度数据的坐标含义

B3 默认题目给出的 `Z_camera` 是相机坐标系中的深度，即沿相机 Z 轴的距离。若实际相机输出的是沿视线方向的距离，而不是 Z 深度，反投影公式需要相应调整；本题按题面给出的反投影公式实现。

## 13. 最终提交说明

最终提交应至少包含：

1. 可运行代码。
2. `README.md` 说明文档。
3. B1-B3 的计算输出和误差验证。
4. A1-A2 的运行截图或录屏。
5. A3 的多客户端运行截图或录屏（如果完成）。
6. GitHub/Gitee 仓库地址。
7. 线上访问地址（如果部署到公网）。
